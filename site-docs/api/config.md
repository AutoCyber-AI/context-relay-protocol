# Config API

The unified configuration object loads defaults, environment variables, `crp.config.yaml`, and runtime overrides into a single typed surface.

## CRPConfig

::: crp.config.CRPConfig
    options:
      show_source: false
      filters: ["!^_"]
