# Events API

The `client.events` namespace exposes the observability event bus used by all SDK subsystems. Subscribe to events for telemetry, logging, or custom handling.

## SDK proxy

::: crp.sdk.proxies._EventsProxy
    options:
      show_source: false
      filters: ["!^_"]
