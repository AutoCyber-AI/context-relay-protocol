# Policy API

The `client.policy` namespace exposes safety policy enforcement, named profiles, violation reporting, and risk-level lookups.

## SDK proxy

::: crp.sdk.proxies_extra._PolicyProxy
    options:
      show_source: false
      filters: ["!^_"]
